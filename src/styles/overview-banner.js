import styled from "styled-components";
// import OverViewBanner from "../components/overViewTitle";

const OverViewBannerContainer = styled.div`
  ul {
    li {
      color: red;
    }
  }
`;

export default OverViewBannerContainer;
